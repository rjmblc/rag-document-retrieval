# rag-document-retrieval
A Retrieval-Augmented Generation (RAG) system that ingests enterprise documents, converts them into vector embeddings, stores them in a vector database, and retrieves context-aware responses using semantic search and LLMs
